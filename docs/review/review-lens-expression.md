# 代码审查报告

## 审查范围

- 审查时间：2026-08-24
- 审查目标：`/Users/lyndon./Desktop/browser-plugin/review-lens/` 下全部 24 个 `.js`（`core/` 15 个、`ui/` 3 个、`content/` 2 个、`popup/` 3 个、`background.js`），共 2686 行；整个目录都是审查对象，不区分「本次改动」。**本次只判表意一个领域（19 条条款，逐条核对），其余领域未加载、未判定**——行为与影响面、结构与依赖、单元设计、语句写法、浏览器端运行时另有报告。已跳过：条款未覆盖的文件类型（`manifest.json`、`.css`、`.html`、图标资源）。总行数超过分批阈值（1500 行），但本次只加载一个规则模块、19 条条款对 24 个文件仍可逐文件逐条核对，故一次审完、未分批。
- 审查依据：JS/TS 代码约定，本次覆盖 表意（名字与事实一致、路径命名、函数名、值的命名、常量名、注释）；lint 采信情况：项目刻意零依赖、无 ESLint / tsc 配置（使用者的明确决定），**一条机器信号都拿不到，19 条全部转人工核对**——其中带规则 id 的 9 条（`@typescript-eslint/naming-convention` 覆盖的类型别名/数组后缀/布尔前缀/`Async` 后缀、`custom/no-implementation-suffix-filename`、`ls-lint`、`custom/no-handle-prefix`、`custom/no-generic-param-name`、`custom/status-field-naming`、`custom/require-unit-suffix`、`no-shadow`、`custom/comment-style`）靠肉眼逐文件扫，可靠性明显低于有 lint 的项目：布尔前缀与遮蔽（`no-shadow`）在无类型信息的 `.js` 里要靠追值判断，注释形态靠脚本扫过一轮再人工复核，两类都存在漏判可能；被目标仓库关闭或判为不适用的规则：无（无 lint 配置可关）。

## 结论

表意层没有会算错结果的问题，但**名字与代码的实际内容大面积对不上**，最该先处理三类：一是名字承诺了布尔却不是布尔——`isCodeComment` 返回的是 `position` 对象、`IS_ORIGIN`/`IS_TYPE_NAME` 是正则常量、另有 12 处自己定义的布尔完全没有判断前缀；二是**同一个词指着好几个不同东西**，`note` 一词四义（GitLab 评论 / 时间脊说明 span / 笔记输入框 / 笔记文本字段，同一份文本还另有 `noteText` 一名），`view` 既是「上下/并排」视图模式又是抽屉实例，`target`、`rail`、`status` 各有两三义，grep 任何一个都找不全消费方；三是**两条注释在说假话**：`ui/code-pane.js:73` 声称行号要「减去起始偏移」，而那份集合装的是文件绝对行号、紧邻代码也没做减法，`content/entry.js:181` 讲的去重其实在 `core/thread.js` 就做完了。函数名批量读起来像变量（27 处名词/形容词短语），是量最大但最机械的一类。

**结论有可靠性折扣。** 19 条全部靠人工核对，其中 9 条本可由 lint 确定给出。**建议先接入 lint**：即使扩展产物保持零依赖，也可以把 eslint 只放进 devDependencies 或借外部规范仓库的配置去 lint，扩展目录不会多出文件；接上之后布尔前缀、`Async` 后缀、`handle` 前缀、泛化参数名、注释形态、遮蔽这几类可直接采信机器结论，不必再打这层折扣。

19 条逐条核对后**无违规的 5 条**，在此列明以免读者误以为漏审：目录单复数（`core`/`ui`/`content`/`popup` 领域单数、`icons` 集合复数，合规）；名字不重复类型信息（`originList` 是 DOM 列表元素、`renderCardList` 是列表视图 UI 概念，均命中例外；集合变量一律复数领域名）；目录与文件名 kebab-case（`cards-view`、`code-pane`、`register-origin` 均合规）；函数禁 `handle` 前缀（无 `handleXxx` 函数；`HANDLERS`/`handler` 是名词属性，命中例外）；异步函数不加 `Async` 后缀（全仓 0 处）。

| 级别    | 数量 |
| ------- | ---- |
| 🔴 阻断 | 0    |
| 🟡 应改 | 29   |
| 🔵 优化 | 5    |

违规数以表格行计，同一条约定在同一类位置上的多处合为一行（位置全部列在「文件:行号」列里，便于逐处修）。

## 违规项

| 级别    | 文件:行号                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 违反的约定                                                                       | 现状                                                                                                                                                                                                                                                                                                             | 建议改法                                                                                                                                                                                                                                          |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/identifier.js:17`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/gitlab.js:43`、`:45`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/snapshot.js:37`、`:47`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/store.js:14`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/thread.js:26`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:46`、`:503`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/bootstrap.js:21`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:158`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/settings.js:76` | 布尔量（含变量、配置项、开关、自造的布尔字段）用 is/has/should/can 一类判断前缀   | `worthIt`、`tokenAccepted`、`withToken`、`fits`、`wholeMethod`、`syncScroll`、`outdated`、`syncing`、`echo`、`orphaned`、`on`（`writeSyncScroll: (on) => …`）、`granted` 共 12 处自己定义的布尔都没有前缀；同文件 `core/thread.js:20` 的 `resolved` 沿用 GitLab 契约字段名，属例外                                 | 依次改 `isWorthKeeping`、`isTokenAccepted`、`shouldUseToken`、`doesMethodFit`、`isWholeMethod`、`isSyncScrollOn`、`isOutdated`、`isSyncingScroll`、`isEchoing`、`isOrphaned`、`isSyncScrollOn`、`isGranted`；`syncScroll` 是持久化键、`outdated` 是跨模块字段，改名要同时改写入与读取处 |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/thread.js:7`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/identifier.js:9`、`:10`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/register-origin.js:14`                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `is`/`has` 开头的名字必须真的是布尔判断                                          | `isCodeComment` 以 `&& note.position` 收尾，返回的是 position 对象或 `false`；`HAS_DOT_OR_CALL`、`IS_TYPE_NAME`、`IS_ORIGIN` 是正则常量，名字读作判断（`IS_ORIGIN` 还是导出的，`popup/settings.js:2` 也引用）                                                                                                      | `isCodeComment` 末项包一层 `Boolean(note.position)`；三个正则改 `DOT_OR_CALL_PATTERN`、`TYPE_NAME_PATTERN`、`ORIGIN_PATTERN`，判断交给 `.test()` 表达                                                                                              |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/compare.js:91`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:199`、`:369`、`:578`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:132`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/settings.js:82`                                                                                                                                                                                                                                                                                                                                                                           | 名字读作判断或状态时，装的就该是判断结果，不该是对象、集合或计数                  | `touchedSinceComment()` 读作判断，实为「取评论后动过该文件的提交列表」；`flag` 装的是徽标文案；`touched` 是提交个数；`stored` 装的是卡片 id，而 `:592` 还可能把它赋成布尔 `true`；`building` 是「正在建抽屉」的 Promise；`registered` 是 `{ ok, reason, message }`                                                   | 依次改 `loadCommitsSinceComment`、`badgeText`、`commitCountSinceComment`、`savedCardId`（并让它只装 id）、`drawerBuild`、`registerResult`                                                                                                           |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/page.js:24`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 名字要说清它装的是什么，不靠注释补救                                             | 返回 `{ project: encodeURIComponent(projectPath), projectPath }`，两个字段同名词根、靠第 23 行注释区分；`core/export.js:29` 还要 `decodeURIComponent(source.project)` 才能显示给人看                                                                                                                               | 改 `encodedProject` 或 `projectApiId`，消费点（`core/thread.js:54-58`、`core/compare.js`、`content/entry.js:219`、`:231`、`core/export.js:29`）同步改，第 23 行注释即可删                                                                           |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/compare.js:62`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 函数名要与它实际返回的东西一致                                                   | `buildComparePair` 名为 pair，实际返回三态判别态：同文件 5-19 行写明只有 `changed` 才有第二侧，`unchanged`/`unlocatable` 时 `now: null`；`popup/cards-view.js:20` 的注释又把它叫 `ComparePair`，代码里没有这个名字                                                                                                 | 改 `buildComparison`（`content/entry.js:3`、`:218` 同步），`popup/cards-view.js:20` 注释里的 `ComparePair` 换成代码里的实际名字                                                                                                                    |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/code-pane.js:16`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 名字要说清返回的是哪一种东西，不与同文件另一个概念撞词                            | `changedRows` 返回的是 `Set<行号>`，而同文件 `:69` 的 `row` 指 DOM 行元素；调用处写成 `changed.has(number)`                                                                                                                                                                                                       | 函数改 `collectChangedLineNumbers`，变量改 `changedLineNumbers`                                                                                                                                                                                    |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/related.js:24`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 一个名字在同一处只表示一件东西：数量与集合不共用一个词                            | `.map(([line, hits]) => ({ line, hits: hits.length, … }))`，同一表达式里 `hits` 一个是数组、一个是个数；`ui/code-pane.js:88` 的 `hits` 又指命中的标识符文本数组                                                                                                                                                   | 字段改 `hitCount`，局部数组改 `hitTexts`；渲染侧沿用 `hitTexts`                                                                                                                                                                                    |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:38`、`:122`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 名字要指向它真正保存的那个对象，且不与同文件已占用的词根冲突                      | 本文件 `host` 指抽屉的宿主自定义元素（`:30`、`:103`），而 `hostOverflow` 装的是宿主页 `document.body` 的 overflow 原值；`already` 装的是 dataset 里取回的原值字符串，名字读作布尔                                                                                                                                  | 改 `bodyOverflowBeforeOpen` 与 `savedBodyOverflow`（`:123-125`、`:730-733` 同步）                                                                                                                                                                  |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:173-176`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 外层对象与它的字段不共用一个名字，读者不必靠位置分辨                              | `badgeFor(state)` 里写 `state.state`、`state.thread`；`state` 既是整份渲染状态又是比较判别态，`render`、`renderSpine`、`renderFoot`、`describeSelection` 一路都叫 `state`                                                                                                                                          | 参数统一改 `renderState`，判别态字段改 `compareState`（`core/compare.js` 产出侧与 `:353`、`:174` 同步）                                                                                                                                            |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/attachment.js:29`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/locate.js:38`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:397`、`:491`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/cards-view.js:106`、`:136`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/export.js:76`                                                                                                                                                                                                                                                                                                  | 局部名字也要说清装的是什么，不用形容词或副词占位                                  | `written` 装的是整段匹配到的原文；`candidates` 装的是方法起始行号；`wasAt`/`was` 装的是各代码区的滚动位置；`filter` 装的是当前选中的仓库（同文件 `:118` 的 `filters` 却是筛选条容器），`shown` 是过滤后的卡片；`newestFirst` 装的是排好序的卡片、名字读作排序开关                                                   | 依次改 `matchedText`、`startLines`、`scrollBefore`、`selectedProject`、`visibleCards`、`cardsNewestFirst`                                                                                                                                          |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:43`、`:421`、`:568`、`:606`、`:623`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/cards-view.js:49-51`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/export.js:56`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/thread.js:9`、`:15`                                                                                                                                                                                                                                                                                                                                                      | 一个业务名词在整份代码里只指一个概念；同一份值也只用一个名字                      | `note` 有四义：core 侧是 GitLab 的评论（`toThread(discussion, note)`、`noteId`）、`ui/drawer.js:421` 是时间脊里的说明 span（class `spine-note`）、`:568` 是笔记输入框、`:606`/`:623` 是笔记文本字段，而同一份文本在 `:43` 又叫 `noteText`；`popup/cards-view.js:49` 那个 `note` 段落装的还可能是评论正文（`card.note \|\| card.comment.body`） | GitLab 侧保留契约名 `note`；说明 span 改 `selectionSummary`（与 `describeSelection` 对齐）；输入框改 `noteInput`；卡片字段与内存变量统一叫 `note`、删掉 `noteText` 这个第二名字；`popup/cards-view.js` 那段改 `excerpt`（它本就可能显示评论正文） |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:153-157` 与 `:197`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:218`、`:259` 与 `:269`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:445` 与 `/Users/lyndon./Desktop/browser-plugin/review-lens/core/export.js:16` 与 `/Users/lyndon./Desktop/browser-plugin/review-lens/popup/popup.js:54`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/settings.js:8` 与 `/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:201`                                                                                                                                   | 同一个词只指一个概念，不同的东西不共用一个词                                      | `view` 既是「上下/并排」视图模式（`settings.view`）又是抽屉实例（`const view = await ensureDrawer()`，而同文件 `:131` 已有 `drawer`）；`target` 既是写入内容的容器元素又是要跳转的行号；`rail` 分别指分栏脊、Markdown 代码围栏、空态装饰竖条；`status` 既是设置页的 DOM 元素又是渲染态字段                          | `const view` 改 `drawer`；行号改 `lineNumber`；三处 `rail` 各用本地概念名（`paneSpine`、`fenceMarks`、`decorRail`）；设置页元素改 `statusLine`                                                                                                      |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/compare.js:48`、`:62-88`、`:120-150`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 同一个概念在整份代码里用同一个词，跨端契约已有既定叫法的以契约为准                | 「评论时 / 当前」这两侧同时有两套词：`diffOfBase(oldLines, newLines, thenBase, nowBase)` 一个签名里就并列了两套，文件内 `oldLines`/`newLines` 与 `then`/`now`/`sliceOld`/`sliceNew` 混用；`core/diff.js:25` 用 `thenLines`/`nowLines`，`core/locate.js:31` 用 `oldLines`/`newLines`                                 | 面向读者的两侧统一用产品口径 then/now（界面文案就是「评论时 / 修正后」）；只有直接对应 GitLab 契约字段的地方（`old_line`、`new_path`、`anchorSide: "old"`）保留 old/new                                                                             |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/identifier.js:30`、`:32-35`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 同一个值从产出到消费不换名字                                                     | `locateIdentifiers(identifiers, lines)` 的回调形参与返回字段都叫 `text`，而同文件 `bareName(identifier)` 把同一个值叫 `identifier`；`extractIdentifiers` 返回 `{ text }` 对象，两个调用方（`core/related.js:8`、`ui/drawer.js:681`）还要 `.map((item) => item.text)` 转回字符串                                     | 整条链路统一叫 `identifier`；顺带让 `extractIdentifiers` 直接返回字符串数组，两处 `.map(...)` 可一并去掉                                                                                                                                            |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:348`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 判别态的取值名与渲染层的类名、容器名用同一个词                                    | 判别态是 `COMPARE_STATE.unchanged`、文案写「这段代码至今没有改动」，而容器 class 是 `untouched`，且 `unlocatable` 那一支也复用同一个 class                                                                                                                                                                         | class 改 `no-code`（与函数名 `renderNoCode` 一致）或 `unchanged`，`ui/drawer.css` 同步                                                                                                                                                             |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/export.js:12`、`:16`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 同一个概念在同一文件里不换词                                                     | `fence(code)` 产出的围栏，在 `codeBlock` 里接成了 `rail`                                                                                                                                                                                                                                                          | 变量改 `fenceMarks`，一处一个词                                                                                                                                                                                                                    |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/register-origin.js:27`、`:52`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 正反成对的操作用对称的名字，且名字要覆盖它实际做的事                              | 注册叫 `registerOriginScripts`、注销叫 `unregisterOrigin`，而后者除了注销脚本还撤回了站点权限；消息协议里又是 `registerOrigin`/`unregisterOrigin`                                                                                                                                                                 | 成对命名 `registerOriginScripts` / `unregisterOriginScripts`，撤权那步单独一个 `revokeOriginAccess`；或整体改 `enableOrigin` / `disableOrigin` 并与 `MESSAGE_ACTION` 对齐                                                                           |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/related.js:25` 与 `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:650-652`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 一份数据从产出到渲染不换名字                                                     | 候选行的依据字段叫 `reason`，渲染时变量与 class 都改叫 `why`                                                                                                                                                                                                                                                      | 渲染侧沿用 `reason`（变量与 class 都用 `reason`），`ui/drawer.css` 同步                                                                                                                                                                            |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/messages.js:1`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 承载单个实体的文件用单数，纯函数工具箱才用复数                                    | 文件承载的是「消息协议」这一个实体（一个 `MESSAGE_ACTION` 常量 + 一个 `ask` 函数），而同目录承载实体的文件都是单数：`attachment.js`、`identifier.js`、`thread.js`、`page.js`、`store.js`                                                                                                                           | 改 `core/message.js`，5 处 import（`content/entry.js`、`popup/popup.js`、`popup/settings.js`、`background.js`）同步                                                                                                                                 |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/popup/cards-view.js:1`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 文件名用业务名词，不挂 UI 形态或代码形态后缀                                      | `cards-view.js` 用 `-view` 这个 UI 形态词命名（且用了复数），它导出的是卡片列表视图 `renderCardList`                                                                                                                                                                                                              | 改 `popup/card-list.js`（业务集合名，与 `renderCardList` 对齐），`popup/popup.js:3` 的 import 同步                                                                                                                                                 |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/identifier.js:30`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/highlight.js:51`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/locate.js:18`、`:20`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/related.js:7`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/register-origin.js:16`、`:18`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/store.js:19`、`:29`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/gitlab.js:36`、`:99`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/snapshot.js:59`、`:67`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/export.js:7`、`:15`、`:22`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/compare.js:34`、`:48`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:173`、`:483`、`:606`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:119`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/cards-view.js:8`、`:28`、`:35`、`:88`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/failure.js:50` | 函数以动词开头，不用名词或形容词短语命名（否则读起来像变量）                      | 27 处函数名是名词/形容词短语：`bareName`、`wordKind`、`nameOf`、`methodStartsNamed`、`relatedLines`、`patternFor`、`scriptIdFor`、`sameThread`、`serial`、`kindOf`、`cached`、`methodNameAt`、`codeOnly`、`fence`、`codeBlock`、`section`、`commitsSince`、`diffOfBase`、`badgeFor`、`codePanes`、`cardFrom`、`withLocation`、`miniPair`、`tag`、`cardRow`、`emptyState`、`actionButton` | 依次改 `stripCallArgs`、`classifyWord`、`readMethodName`、`findMethodStartsNamed`、`findRelatedLines`、`toMatchPattern`、`toScriptId`、`isSameThread`、`runSerially`、`toErrorKind`、`readThroughCache`、`readMethodNameAt`、`stripStringsAndComments`、`buildFence`、`wrapInCodeBlock`、`toSectionMarkdown`、`loadCommitsSince`、`diffBaseRanges`、`pickBadgeText`、`queryCodePanes`、`buildCard`、`addCardLocation`、`renderMiniPair`、`createTag`、`renderCardRow`、`renderEmptyState`、`createActionButton` |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/store.js:19`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/failure.js:50`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:63`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/compare.js:82`、`:143`                                                                                                                                                                                                                                                                                                                                                                                        | 函数参数用具体领域词，不用空泛占位名                                              | `sameThread(a, b)` 的两个参数是卡片来源；`actionButton(label, handler)` 的 `handler` 说不出这个按钮点了做什么；`attachTo(box)` 的 `box` 是讨论容器；`sliceOld(extra)`/`sliceNew(extra)` 的 `extra` 填进去的正是 `extraLines`                                                                                        | 依次改 `(source, other)`、`onActivate`、`discussionBox`、`extraLines`                                                                                                                                                                              |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/snapshot.js:88`、`:98`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 命名用肯定式，不用「不是什么」当名字                                             | `NOT_A_SIGNATURE` 匹配的其实是 `if`/`for`/`while`/`switch`/`catch`/`class` 等控制语句与类型声明的起始行；判断处写成 `NOT_A_SIGNATURE.test(line) \|\| !SIGNATURE.test(line)`，一个否定名叠一个取反                                                                                                                       | 改 `CONTROL_OR_DECLARATION_HEADER`，条件保持不变即可读通：「是控制语句头，或不匹配签名形态，就跳过」                                                                                                                                                |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:170`、`:174`、`:187`、`:199`、`:201`、`:216`、`:238`、`:248`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 异步状态字段的名字要指名它服务的那份数据，不用泛化词占位                          | `loadError` 用 `load` 这个动作词占位，同作用域里没有叫 `load` 的数据（数据是 `threadsByDiscussion` 与 `mergeRequest`）；渲染态字段是裸 `status`，认不出它说的是哪份数据的取数状态（`ui/drawer.js:705`、`:713` 依它分支）                                                                                           | `loadError` 改 `threadsError`；`status` 改 `compareStatus` 并同步 `ui/drawer.js` 两处判断                                                                                                                                                          |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/store.js:13`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:155`、`:156`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 跨模块传递的尺寸类数值字段，名字要带单位后缀                                      | `drawerWidth` 是像素值，单位只活在使用处的 `${width}px` 里；它是持久化到 `chrome.storage.local` 的字段，popup、content script、service worker 三侧都读（`ui/drawer.js:12-14`、`:37`、`:80-81` 是同一个量）                                                                                                          | 字段改 `drawerWidthPx`；因为改的是持久化键，要么在 `readSettings` 里兼容旧键，要么接受存量用户回落到默认宽度——动手前先确认取哪种                                                                                                                   |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/page.js:7`、`:18`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:94`、`:95`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/settings.js:8`                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 内层作用域不重用外层（含全局）已占用的名字                                        | `isMergeRequestPage(document, location)`、`parseMergeRequestRef(location)` 的形参与全局 `document`/`location` 同名；`init({ origin, location })` 注入了这两个值，但函数体 `:146`、`:230` 仍直接读 `window.location.origin`，同一函数里两种来源并存、读者无法从名字判断某处用的是注入值还是全局值；`popup/settings.js:8` 的模块级 `status` 与 `window.status` 同名 | 形参改 `pageDocument`/`pageLocation`（调用点 `content/bootstrap.js:12`、`content/entry.js:108` 同步）；`init` 内部一律用注入的 `origin`；DOM 元素改 `statusLine`                                                                                    |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/code-pane.js:73`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 注释不能与代码说反；改逻辑时同步更新，宁可删掉过时注释                            | 注释写「`changed` 里存的是「本侧第几行」，快照行号减去起始偏移即得」，而 `changed` 装的是文件绝对行号（`core/compare.js:54-58` 已换算），紧邻的 `changed.has(number)` 也没有做任何减法；同文件 `:13-14` 的注释说的才是对的                                                                                          | 删掉这一行（`:13-14` 已交代口径），或改写成「行号已是文件绝对行号，可直接对号」                                                                                                                                                                    |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:181`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 注释只讲这段代码自己的事，不复述别处模块的实现                                    | 注释写「一个主题可能有多条评论，取第一条带 position 的即可定位代码」，但这件事在 `core/thread.js:67-69` 就做完了（每个 discussion 只产出一条），这里的 `if (!threadsByDiscussion.has(...))` 只是又去重一次                                                                                                         | 删掉这条注释，并一并评估这层已无作用的去重判断；要留就改成「`loadThreads` 已保证每个讨论一条，这里只建索引」                                                                                                                                       |
| 🟡 应改 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/store.js:85`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 注释只写代码表达不了的部分，不复述紧邻代码已说明的事实                            | 注释「三个写入口全部走队列」，紧跟的三行就是 `serial(() => …)`，读者从代码直接读得出                                                                                                                                                                                                                              | 删掉；要留就写代码读不出的那半句——为什么读入口（`listCards`、`findCard`、`readSettings`）不排队                                                                                                                                                    |
| 🔵 优化 | `/Users/lyndon./Desktop/browser-plugin/review-lens/core/identifier.js:8`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/page.js:2`、`:5`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/attachment.js:9`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/highlight.js:8`、`:18`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/thread.js:42`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/gitlab.js:17`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/snapshot.js:27`、`/Users/lyndon./Desktop/browser-plugin/review-lens/core/store.js:6`、`:7`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/code-pane.js:10`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/failure.js:7`、`:62`、`/Users/lyndon./Desktop/browser-plugin/review-lens/popup/cards-view.js:6`、`/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:12`、`:13`、`:14`、`:320`、`/Users/lyndon./Desktop/browser-plugin/review-lens/background.js:25` | 顶层常量名要含业务属性、单位与边界，不截断核心上下文                              | `CANDIDATE`、`MERGE_REQUEST_PAGE`（data-page 取值）与 `MERGE_REQUEST_PATH`（正则）、`IMAGE`、`KEYWORDS`、`RULES`、`PER_PAGE`（`core/compare.js:32` 同类的却叫 `COMMITS_PER_PAGE`）、`KIND_BY_STATUS`、`WHOLE_METHOD_LIMIT`、`CARDS`/`SETTINGS`（存储键）、`CHANGE_ON`、`EXPLANATION`/`UNKNOWN`、`ALL`、`MIN_WIDTH`/`MAX_WIDTH`/`DEFAULT_WIDTH`（缺业务属性与单位）、`RECENT_COMMITS`（缺边界）、`HANDLERS` | 依次改 `IDENTIFIER_CANDIDATE_PATTERN`、`MERGE_REQUEST_DATA_PAGE`、`MERGE_REQUEST_PATH_PATTERN`、`MARKDOWN_IMAGE_PATTERN`、`JAVA_KEYWORDS`、`TOKEN_RULES`、`DISCUSSIONS_PER_PAGE`、`ERROR_KIND_BY_HTTP_STATUS`、`MAX_WHOLE_METHOD_LINES`、`CARDS_STORAGE_KEY`、`SETTINGS_STORAGE_KEY`、`CHANGE_TYPE_BY_SIDE`、`EXPLANATION_BY_ERROR_KIND`、`UNKNOWN_EXPLANATION`、`ALL_PROJECTS`、`MIN_DRAWER_WIDTH_PX`/`MAX_DRAWER_WIDTH_PX`/`DEFAULT_DRAWER_WIDTH_PX`、`MAX_RECENT_COMMITS`、`HANDLER_BY_ACTION` |
| 🔵 优化 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:61-65`、`:114-121`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 连续几段注释共享同一段后继代码时合并成一个块注释（非导出位置用 `/* */`）          | `:61-65` 是一段 `/* */` 紧跟一段 `//`、中间无空行，共同挂在 `let releaseDrag = null;` 上；`:114-121` 是两段 `/* */` 相邻，共同挂在 `const already = …` 上。两处都属于按注释类型分路的扫描器查不出的混合堆叠（与已另行记录的 `core/compare.js:25-31` 同类）                                                          | 各自合并成一个 `/* */`                                                                                                                                                                                                                            |
| 🔵 优化 | `/Users/lyndon./Desktop/browser-plugin/review-lens/popup/popup.js:15`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:8`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `/** */` 只用于导出声明，文件内部实现与文件级说明不用 JSDoc 形态                  | `popup/popup.js:15` 的 `/** … */` 注的是函数体内的 `exportButton.hidden = …`；`content/entry.js:8` 的 `/** … */` 是文件级说明（其余 23 个文件同类说明都用 `/* */`），且与下方的 export 之间空了一行，读者对不上它注释的是谁                                                                                         | `popup/popup.js:15` 改 `//`；`content/entry.js:8` 改 `/* */`                                                                                                                                                                                       |
| 🔵 优化 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:418-420`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 一行说得完的注释不包成块注释                                                     | 块里只有一句「选择逻辑不该靠猜：直接写出在看哪一段、多少行、有几行改动（DD-32）。」                                                                                                                                                                                                                               | 非导出位置改成一行 `//`                                                                                                                                                                                                                            |
| 🔵 优化 | `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/drawer.js:482`、`/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:167`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 注释挂在它所说明的那段代码上方                                                    | `ui/drawer.js:482` 的「两栏滚动同步；echo 标志挡住回弹」挂在 `const codePanes = …` 上方，而 `echo` 在 20 行之后的 `linkScroll` 里、那里已有自己的注释；`content/entry.js:167` 的「取讨论失败时仍然挂入口」挂在 `let threadsByDiscussion = new Map();` 上方，说的是 `:257-263` 那次 `attachEntries` 的兜底            | 各自移到它说明的那段代码上方（`linkScroll` 的 `echo` 处、`attachEntries` 调用处）                                                                                                                                                                  |

## 既有债务

范围是指定目录、不区分「本次改动」，故本段只记**超出表意模块判定范围**、审查中顺带看到的点，**不计入上表违规数**：

- `/Users/lyndon./Desktop/browser-plugin/review-lens/core/compare.js:48`：`diffOfBase(oldLines, newLines, thenBase, nowBase)` 的前两个参数在函数体内一次都没用到（死参数，属单元设计领域）。
- `/Users/lyndon./Desktop/browser-plugin/review-lens/ui/failure.js:33-37`：`unexpected` 那一条缺 `canUseToken` 字段，其余 5 条都有（形态兜底，属单元设计领域）。
- `/Users/lyndon./Desktop/browser-plugin/review-lens/content/entry.js:146`、`:230`：`init` 注入了 `origin` 参数，这两处却直接读 `window.location.origin`，注入实际上被绕过（属行为层，表意上的同名问题已记在上表遮蔽那一条里）。
- `core/` 这个目录名描述的是「代码在哪一层」而不是领域，其下 15 个文件里 `diff.js` / `locate.js` / `related.js` / `compare.js` / `highlight.js` 都是动作名文件，按约定动作名文件要待在限定了领域、且工序有限不重叠的目录里（`locate` 与 `related` 都在做「找行」）。这条落不到具体行号上，按红线不写成违规项，仅在此提示。
- 抽屉默认宽度 820 在 `core/store.js:13` 与 `ui/drawer.js:14` 各写一份、`900` 毫秒闪烁时长有三份副本——已记在 `review-lens-conventions.md`，此处不重复计数。

## 未能确认的项

- `drawerWidth` 改成 `drawerWidthPx` 是改持久化键，存量用户已存的 `drawerWidth` 会读不到而回落默认宽度。要不要在 `readSettings` 里做一次旧键兼容，取决于是否在意存量用户的宽度丢失——需要作者定。`syncScroll` 改成 `isSyncScrollOn` 同理。
- `MESSAGE_ACTION` 的取值（`registerOrigin`/`unregisterOrigin` 等）是 content script、service worker、popup 三侧共享的字符串协议。上表里涉及成对命名的建议只改函数名即可，若要连动作名一起改，需三处同步且扩展更新后旧 content script 仍在页面上时会短暂对不上——是否连动需要作者定。
