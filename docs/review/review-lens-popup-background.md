# 代码审查报告

## 审查范围

- 审查时间：2026-08-24
- 审查目标：指定文件，共 5 个、760 行——`review-lens/popup/popup.js`(62)、`review-lens/popup/cards-view.js`(149)、`review-lens/popup/settings.js`(116)、`review-lens/popup/popup.css`(373)、`review-lens/background.js`(60)。未剔除任何文件（范围内全是 JS 与 CSS）。参考但不作为审查对象、不计违规：`core/messages.js`、`core/store.js`、`core/register-origin.js`、`core/export.js`、`popup/popup.html`、`popup/settings.html`、`content/entry.js`（另有批次负责，本报告仅引用它们佐证契约）。
- 审查依据：JS/TS 代码约定，本次覆盖 结构与依赖、单元设计、语句写法、表意、样式（CSS）、浏览器端运行时；**行为与影响面由行为层批次单独覆盖，本批未判**。未加载 TypeScript 与 React 两组条款（项目为纯 JS、无框架）。lint 采信情况：项目按 TD-11 刻意零依赖、无构建、无 lint 配置，`eslint`/`stylelint`/`tsc` 均跑不起来，**全部条款转人工核对**——带规则 id 的条款（导入分组、参数解构、布尔命名、注释形态、块内声明次序等）靠肉眼扫，可靠性低于有 lint 的项目，本报告的形态类结论应按此打折。被判为不适用的规则：样式嵌套层级（纯 CSS 无嵌套语法）、样式规则块对齐 JSX（无 CSS Modules、无 JSX）、导入路径别名（项目未配别名，相对路径是唯一选择）、按文件后缀决定导出形式（无 `.ts`/`.tsx`）。

## 结论

五个文件的错误出口做得比同类代码好——弹窗、设置页、service worker 三处失败都有面向用户的话可说，删除失败会把按钮解禁并改文案，注册失败会回滚权限，这些是本次没有可挑之处的部分。问题集中在三处：一是**设置页在读到设置之前入口就已可交互**，回车触发的 `addOrigin()` 又是一个既不 `await` 也不 `void` 的游离 Promise，其守卫段落在 `try` 之外，此时点击「保存」还会把空输入框当成「清空令牌」写回存储——这是本批唯一的阻断项，建议先处理；二是**卡片列表把筛选状态藏在渲染函数里**，调用方每次重绘都会把它重置，删除一张卡片后筛选条跳回「全部」；三是形态类的批量偏离，签名解构（8 处）、名词命名的工厂函数（4 处）、消息表列举顺序与协议声明顺序不一致各占一批。样式表整体守着 recess-order，只有三个块跑偏。

另有一条**已确认但归属行为层**、本批不判的缺陷：弹窗展示仓库名时读的是 `source.project`（`encodeURIComponent` 后的 API id），而 `content/entry.js:117` 已写明展示应用 `projectPath`；详见「未能确认的项」，请与行为层批次合并处理。

lint 折扣的处置：项目零依赖是已作出的决定（TD-11），本报告不重复建议引入 devDependencies；但要如实说明——上表中形态类条目（导入分组、参数解构、注释形态、块内声明次序、布尔前缀）本应由 `import/order`、`custom/no-param-destructuring`、`custom/comment-style`、`order/properties-order`、`naming-convention` 机器判定，此处全为人工核对，可能有漏。若日后放开 devDependencies，这五条是最值得优先接入的。

| 级别    | 数量 |
| ------- | ---- |
| 🔴 阻断 | 1    |
| 🟡 应改 | 17   |
| 🔵 优化 | 6    |

## 违规项

| 级别    | 文件:行号                                                    | 违反的约定                                                             | 现状                                                                                                                                                       | 建议改法                                                                                                                     |
| ------- | ------------------------------------------------------------ | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 🔴 阻断 | `review-lens/popup/settings.js:103,105`                      | 每个 Promise 都要被消费：`await`、接 `catch`、或显式 `void` 声明不等   | 回车分支 `if (event.key === "Enter") addOrigin();` 裸调 async 函数；`addOrigin` 的前两道守卫（`originInput.value` 读取、`settings.extraOrigins.includes`）在 `try` 之外，设置尚未读到时 `settings` 为 `null`，抛出的 TypeError 落在无人接管的 rejection 里，界面不会有任何提示 | 把归一化与两道守卫也纳入 `try`（或让 `addOrigin` 内部全程自 catch），回车分支写成 `void addOrigin();`，并在 `catch` 里 `say(...)` 给出提示 |
| 🟡 应改 | `review-lens/popup/cards-view.js:105`                        | 一个单元只做一件事                                                     | `renderCardList` 既渲染列表又持有筛选状态（`let filter = ALL`）；调用方 `popup.js:23` 删除后重新调用它，`filter` 随之重置——用户按仓库筛选后删一张卡，筛选条跳回「全部」 | 筛选态交给调用方持有（作为参数与 `onFilterChange` 回调传入），或在模块内持有一次、只暴露更新数据的入口，使重绘不重置用户选择 |
| 🟡 应改 | `review-lens/popup/settings.js:49,103`                       | 本地状态恢复完成前，界面不能按未就绪的值工作                           | 三个入口（保存、添加、回车）在模块顶层 `await ask(readSettings)`（109 行）之前就已绑定；此窗口内点「保存」会把空的 `tokenInput.value` 当成「留空即清空」写回，抹掉已存令牌 | 读到设置后再绑定监听（或在处理器首句判 `settings === null` 直接 `say("正在读取设置…")` 返回），并让输入框在恢复完成前保持禁用 |
| 🟡 应改 | `review-lens/popup/settings.js:85`                           | `catch` 必须有实质处置，不留空块吞掉异常                               | `await chrome.permissions.remove(...).catch(() => {})`：注册失败后的权限回滚若自己也失败，用户处于「已授权、无脚本、列表里也没有」的状态，且无任何提示与日志 | 改 `try/catch`，失败时 `console.warn` 并把这层信息并进 `say(...)`（如「注册失败，且权限未能回收，请到扩展权限页手动移除」）    |
| 🟡 应改 | `review-lens/background.js:25-40`                            | 同一批成员在多处被逐个列举时，各处顺序保持一致                         | `HANDLERS` 的键序为 …`writeSettings`、`openSettings`、`pageActive`、`registerOrigin`、`unregisterOrigin`，而协议声明处（`core/messages.js`）是 …`writeSettings`、`registerOrigin`、`unregisterOrigin`、`openSettings`、`pageActive` | 把 `HANDLERS` 的键序调成与 `MESSAGE_ACTION` 声明顺序逐字一致，新增 action 时两处同位置插入                                    |
| 🟡 应改 | `review-lens/popup/cards-view.js:35,105`                     | 入参在函数体首部解构，参数列表只保留完整形参名                         | `function cardRow(card, { onDelete })`、`export function renderCardList(root, { cards, onDelete })` 在签名里铺开字段                                        | 签名收为 `(card, options)` / `(root, options)`，在函数体第一行解构                                                            |
| 🟡 应改 | `review-lens/background.js:27,28,29,31,38,39`                 | 入参在函数体首部解构，参数列表只保留完整形参名                         | 六个 handler 都写成 `({ source }) => …`、`({ patch }) => …` 形式的签名解构                                                                                  | 改为 `(message) => store.findCard(message.source)` 之类的整体形参，或函数体首行解构；因是一行转发，取整体形参最省             |
| 🟡 应改 | `review-lens/popup/popup.js:52-61`                           | 相同或近似的逻辑块在 2 处以上出现时抽成公共实现，单处维护               | 这段错误态 DOM（`div.empty` > `.empty-rail` + `strong` + `p`）与 `cards-view.js:88` 的 `emptyState` 结构、类名完全同构，且此处 rail 用 `div`、那边用 `span`  | 把 `emptyState(title, detail)` 提为导出函数，popup 的错误态复用它，rail 的元素类型随之统一                                    |
| 🟡 应改 | `review-lens/popup/popup.js:13`                              | 相同的取值—转换套路在 2 处以上出现时抽成公共实现                       | 「卡片里有哪些仓库」在这里算一遍取 `size`，在 `cards-view.js:117` 又算一遍取列表，两处都是 `new Set(cards.map((card) => card.source.project))`               | 抽一个 `projectsOf(cards)` 放在 `cards-view.js` 并导出（或放共享模块），两处引用同一实现                                      |
| 🟡 应改 | `review-lens/popup/cards-view.js:23`                         | 一个声明放在紧邻它第一个消费者之前，被引用者在前、引用者紧随其后        | `tag` 只被 `miniPair` 消费，却定义在 `miniPair`（第 8 行）之后                                                                                              | 把 `tag` 移到 `miniPair` 之前                                                                                                |
| 🟡 应改 | `review-lens/popup/cards-view.js:6`                          | 文件顶部只留全文件都要的声明，其余贴着第一个消费者                     | `ALL` 摆在文件头，唯一消费者是第 105 行的 `renderCardList`，中间隔着 `miniPair`/`tag`/`cardRow`/`emptyState` 四个与它无关的函数                              | 下移到 `renderCardList` 上方                                                                                                 |
| 🟡 应改 | `review-lens/popup/cards-view.js:8`                          | 函数只接收它真正用到的数据，不为取一两个字段传整个大对象               | `miniPair(card)` 只读 `thenCode` 与 `nowCode`，却收下整张卡片                                                                                               | 改签名为只收这两个值（`miniPair(options)`，体内解构 `thenCode`/`nowCode`），调用处传 `{ thenCode: card.thenCode, nowCode: card.nowCode }` |
| 🟡 应改 | `review-lens/popup/settings.js:64`                           | 一个函数要么做编排、要么做细节，不把两个抽象层级混在一处               | `addOrigin` 主流程调的都是具名步骤（`requestOriginAccess`/`ask`/`say`/`drawOrigins`），中间夹着 `originInput.value.trim().replace(/\/$/, "")` 与正则校验两条裸细节 | 抽 `normalizeOrigin(rawOrigin)`（含去空白与去尾斜杠），`addOrigin` 只留「取值 → 校验 → 授权 → 注册 → 落盘 → 刷新」            |
| 🟡 应改 | `review-lens/popup/cards-view.js:8,23,35,88`                 | 产生副作用或执行动作的函数以动词开头                                   | `miniPair`、`tag`、`cardRow`、`emptyState` 四个都是名词命名的元素工厂，读起来像变量                                                                          | 统一动词前缀：`createMiniPair` / `createTag` / `createCardRow` / `createEmptyState`（或全用 `render` 前缀，与已有 `renderCardList` 对齐） |
| 🟡 应改 | `review-lens/popup/settings.js:76`                           | 布尔量用 `is`/`has`/`should` 前缀，读作判断短语                         | `const granted = await requestOriginAccess(...)`，随后 `if (!granted)`                                                                                       | 改名 `isGranted`                                                                                                             |
| 🟡 应改 | `review-lens/background.js:49,55,56`                         | 本侧自己定义的布尔字段一律带 `is` 前缀，包括响应体里自造的布尔         | 回包信封用 `{ ok: true, result }` / `{ ok: false, error }`，`ok` 是本侧自造的布尔字段而非跨端既定契约                                                        | 改 `isOk`，并同步 `core/messages.js` 的 `ask`（`if (!reply?.isOk)`）；`core/register-origin.js` 的返回值是另一套信封，需一并确认口径 |
| 🟡 应改 | `review-lens/popup/popup.js:1-3`                             | 导入按来源分组，组间留空行、组内按字母升序                             | `../core/messages.js` 排在 `../core/export.js` 之前，且 parent 组与 sibling 组（`./cards-view.js`）之间没有空行                                              | 调整为 `export.js` → `messages.js`，空一行再写 `./cards-view.js`                                                             |
| 🟡 应改 | `review-lens/popup/settings.js:49`                           | 两个单元靠同一个字符串协作时，该约定收敛成一处具名常量                 | 其余五个 DOM id 都在文件顶部一组 `const` 里收口，只有 `document.getElementById("save")` 的 `"save"` 裸在中段，与 `settings.html` 的这条约定散在两处          | 顶部补 `const saveButton = document.getElementById("save");`，与其它 id 查询并列                                              |
| 🔵 优化 | `review-lens/popup/popup.js:15`                              | 导出声明用 `/** */`，文件内部实现用 `//`；非导出位置用 JSDoc 即为越权   | `draw()`（非导出函数）体内的说明写成 `/** 没有卡片时导出按钮…（DD-28） */`                                                                                   | 改为单行 `//` 注释                                                                                                           |
| 🔵 优化 | `review-lens/popup/cards-view.js:35`                         | 同一前缀在同一作用域被读到第二次就解构                                 | `cardRow` 体内读 `card.symbol`、`card.source.project`、`card.note`、`card.comment.body`、`card.source.webUrl`、`card.id`，六次点号前缀                        | 函数体首部 `const { symbol, source, note, comment, id } = card;`，`source` 再按需取 `project`/`webUrl`                        |
| 🔵 优化 | `review-lens/popup/cards-view.js:6`                          | 顶层常量名含业务属性与边界，不截断核心上下文                           | `const ALL = "__all__"` 只说「全部」，说不出是哪一维的全部                                                                                                  | 改名 `ALL_PROJECTS`                                                                                                          |
| 🔵 优化 | `review-lens/background.js:54-56`                            | 异步流程统一 `async/await`，不用 `.then()`/`.catch()` 链               | `handler(message, sender).then(...).catch(...)`                                                                                                              | 监听器本身必须同步返回 `true`，改法是内嵌一个 async 函数（`const reply = async () => { try { respond({ ok: true, result: await handler(...) }) } catch … }; void reply();`），`return true` 保留 |
| 🔵 优化 | `review-lens/popup/popup.css:82,89,187,251,259`              | 反复出现的色值提取为变量，不在各处硬编码                               | `#fff` 硬编码 5 处，而 `:root` 已有同色的 `--card: #ffffff`（第 6 行），两种写法并存                                                                        | 统一用 `var(--card)`；若「卡片底色」与「按钮/反白文字」语义不同，另立 `--surface` / `--on-mark` 变量再引用                    |
| 🔵 优化 | `review-lens/popup/popup.css:98-104,291-295,360-364`         | 规则块内的声明按 recess-order 分组次序书写                             | `.card` 的 `overflow: hidden` 落在 `background`/`border` 之后（应在 `margin` 之后、视觉属性之前）；`.field` 与 `.origins` 都把 `margin` 写在 `padding` 之前   | `.card` 的 `overflow` 上移到 `margin-bottom` 之后；`.field`、`.origins` 交换 `padding` 与 `margin` 两行。文件其余 30 余个块次序正确，可对照它们 |

## 既有债务

- `review-lens/popup/popup.css:321-322`：`/* 解释退到次要位置：比标签小、比输入框轻 */` 描述的是 333 行的 `.field-note`，却贴在 `.field-row` 上方，与下一行 `/* 输入框与按钮同行… */` 连成一段共享同一段后继代码的多段注释（中间无空行）。读者会把第一句当成 `.field-row` 的说明。本次加载的条款里，注释形态与位置只由 JS/TS 侧的表意条款管，样式条款组没有对应条款，故不计入违规数。**建议在样式规则模块增补一条「注释形态与位置」条款**（与 JS 侧同构：注释独占目标规则块上方、多段合并成一段、不描述别处的规则块）；按审查纪律，规则文件不在本次审查里就地改动。

## 未能确认的项

- **仓库名展示读错字段（已确认缺陷，但归属行为层，本批不判 G1，请与行为层批次合并处理）**：`review-lens/popup/cards-view.js:46` 与 `:117,121,122`、以及 `review-lens/popup/popup.js:13` 都取 `card.source.project`。该字段在 `core/page.js:24` 是 `encodeURIComponent(projectPath)`（如 `group%2Fsub%2Frepo`），`content/entry.js:117` 已明确写下「展示要用 `projectPath`」，且卡片 `source` 里确实带着 `projectPath`（`content/entry.js` 的 `withLocation` 展开了 `...ref`）。后果有两处：`.card-repo` 显示编码后的路径；筛选 chip 的 `label.split("/").at(-1)` 在编码字符串上永不切分，标签会铺满整条路径。需要行为层批次确认历史卡片是否都带 `projectPath`（决定要不要保留 `decodeURIComponent(project)` 兜底，`core/export.js:29` 就是这么兜的），再决定改法。
